v3 is the transitional reboot kernel.

It remains available for continuity while the v4 publish stack is built, but v3
outputs are not the final publish contract. Do not add new canonical publish
fields here; use `scripts/v4/` for the v4 manifest, schemas, release gates, and
canonical output path.
